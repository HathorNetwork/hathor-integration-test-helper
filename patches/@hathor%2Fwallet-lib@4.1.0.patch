diff --git a/lib/utils/bigint.js b/lib/utils/bigint.js
index eeb7f4a95a0017693e0b1b2656a133b6a85f41b4..b2ef591a08eaa58f1d46df4a36db6c9caadac3f4 100644
--- a/lib/utils/bigint.js
+++ b/lib/utils/bigint.js
@@ -50,7 +50,7 @@ const JSONBigInt = exports.JSONBigInt = {
       // Otherwise, we can keep it as a Number.
       return value;
     } catch (e) {
-      if (e instanceof SyntaxError && (e.message === `Cannot convert ${context.source} to a BigInt` || e.message === `invalid BigInt syntax`)) {
+      if (e instanceof SyntaxError && (e.message === `Cannot convert ${context.source} to a BigInt` || e.message === `invalid BigInt syntax` || e.message === `Failed to parse String to BigInt`)) {
         // When this error happens, it means the number cannot be converted to a BigInt,
         // so it's a double, for example '123.456' or '1e2'.
         return value;
